SharingDataJava - 

Running on one resource group with 1 host, 5 slots.

The applicatin contains two packages: SampleApp and SharingData

There are two service type corresponding to the packages, SharingData is the default service
And there are two session types

The client will create 2 sessions according to the two session types
Service standby is enabled


Expected observation:

SharingData will be standbby


[3/12/14] Add a third package based on SampleApplicationJava
